spring:
  config:
    activate:
      on-profile: h2
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:mpls-food-test
    username: sa
    driverClassName: org.h2.Driver